# src

这里是 React 前端最核心的源码目录。

## 子目录作用

- `assets/`：图片、图标、字体等资源
- `components/`：可复用组件
- `layouts/`：页面整体布局
- `pages/`：具体页面
- `router/`：页面跳转规则
- `store/`：全局状态管理
- `services/`：请求后端、调用云函数
- `hooks/`：可复用逻辑
- `utils/`：工具函数
- `styles/`：全局样式和主题变量
- `constants/`：常量配置
- `mock/`：模拟数据
