# scholar-network

`scholar-network` 云函数负责返回学者图谱的基础数据。

当前默认动作：

- `graph`：读取 `scholar_network` 集合中的默认图谱文档

说明：

- 这个函数目前先承担“读图谱”职责，保证学者网络页面可以直接从 CloudBase 取数。
- 当前用户的本地加入网络和上传论文逻辑仍然保留前端演示层，避免在没有正式登录体系时引入过多身份同步问题。
